<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:background="#ea191919"
    tools:context="ocar.ocarandroidapp.SplashScreen"
    android:rowCount="3"
    android:columnCount="3"
    android:id="@+id/splash_screen_root_layout">

    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->


    <!-- This FrameLayout insets its children based on system windows using
         android:fitsSystemWindows. -->

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/splash_screen_heading"
        android:text="@string/splash_screen_heading"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:textColor="#09afdf"
        android:textSize="@dimen/abc_action_bar_progress_bar_size"
        android:textStyle="italic" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/logo_bg"
        android:layout_marginBottom="30dp"
        android:layout_above="@+id/splash_screen_heading"
        android:layout_centerHorizontal="true" />

    <ProgressBar
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/progressBar"
        android:layout_below="@+id/splash_screen_heading"
        android:layout_alignLeft="@+id/splash_screen_heading"
        android:layout_alignStart="@+id/splash_screen_heading"
        android:layout_marginTop="67dp"
        android:layout_alignRight="@+id/splash_screen_heading"
        android:layout_alignEnd="@+id/splash_screen_heading" />

</RelativeLayout>
